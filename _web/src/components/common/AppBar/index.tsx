import { Typography } from "@mui/material";

// NOTE: When the height is changed, Update the LoadingScreen style.
export const AppBar = () => (
  <>
    <Typography variant="h1">Shamo 🐔</Typography>
  </>
);
